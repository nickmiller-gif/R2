-- Migration version recorded on shared project from a non-R2 channel (empty name in schema_migrations).
-- No-op placeholder so Supabase CLI / GitHub Preview can reconcile local files with remote history.
-- See docs/supabase-github-preview.md
SELECT 1;
