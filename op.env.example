# Copy to op.env (gitignored) or use: op run --env-file=op.env -- node scripts/...
# 1Password vault item: R2 / r2-signal-ingest
#
# Field names in 1Password (NOT credential / R2_SIGNAL_INGEST_HMAC_SECRET — those refs do not exist):
#   hmac, eigen_service_role, url

R2_SIGNAL_INGEST_URL=op://R2/r2-signal-ingest/url
R2_SIGNAL_INGEST_BEARER=op://R2/r2-signal-ingest/eigen_service_role
SUPABASE_SERVICE_ROLE_KEY=op://R2/r2-signal-ingest/eigen_service_role
R2_SIGNAL_INGEST_HMAC_SECRET=op://R2/r2-signal-ingest/hmac
SUPABASE_URL=https://zudslxucibosjwefojtm.supabase.co

# Autonomous bot service tokens live on the Eigen project edge secrets (not in r2-signal-ingest item).
# Resolve via Eigen Dashboard or R2/.env.wave1.local when running verify-autonomous-bots.mjs.
