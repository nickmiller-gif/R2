node_modules
dist
coverage
package-lock.json
# Generated by `supabase gen types typescript` — must stay byte-identical to
# the CLI output so `npm run lint:supabase:types` (`cmp -s`) passes in CI.
# Reformatting the file makes it drift from the CLI serializer and the
# drift check fails on the next push.
database.types.ts
